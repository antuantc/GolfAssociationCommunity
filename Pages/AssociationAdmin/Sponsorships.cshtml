@page
@model GolfAssociationCommunity.Pages.AssociationAdmin.SponsorshipsModel
@{
    ViewData["Title"] = "Manage Sponsorships";
}

<section class="section-panel">
    <div class="admin-page-header">
        <div>
            <h1>Sponsorship Packages</h1>
            <p>Create and maintain sponsorship levels, pricing, and benefit details.</p>
        </div>
    </div>

    @if (TempData["SuccessMessage"] is string success)
    {
        <div class="message success">@success</div>
    }

    <details class="collapsible-form">
        <summary class="collapsible-form__toggle">
            <span>Create Sponsorship Package</span>
        </summary>
    <form method="post" asp-page-handler="Create" autocomplete="on" style="margin-top:1rem;">
        <div asp-validation-summary="ModelOnly" class="message error"></div>
        <div class="card-grid">
            <div>
                <label asp-for="Input.Name">Package Name</label>
                <input asp-for="Input.Name" class="form-control" />
                <span asp-validation-for="Input.Name" class="text-danger"></span>
            </div>
            <div>
                <label asp-for="Input.Amount">Contribution Amount</label>
                <input asp-for="Input.Amount" class="form-control" inputmode="decimal" />
                <span asp-validation-for="Input.Amount" class="text-danger"></span>
            </div>

            <div>
                <label asp-for="Input.IsActive">Status</label>
                <div>
                    <input asp-for="Input.IsActive" /> Active
                </div>
            </div>
        </div>
        <div>
            <label asp-for="Input.Description">Description</label>
            <textarea asp-for="Input.Description" class="form-control" rows="3"></textarea>
            <span asp-validation-for="Input.Description" class="text-danger"></span>
        </div>
        <div>
            <label asp-for="Input.Benefits">Benefits</label>
            <textarea asp-for="Input.Benefits" class="form-control" rows="4" placeholder="List key sponsor benefits and inclusions."></textarea>
            <span asp-validation-for="Input.Benefits" class="text-danger"></span>
        </div>
        <div class="form-actions">
            <button type="submit" class="btn">Create Package</button>
        </div>
    </form>
    </details>
</section>

<section class="section-panel">
    <h2>Current Packages</h2>

    @if (Model.Packages.Count == 0)
    {
        <p>No sponsorship packages created yet.</p>
    }
    else
    {
        <div class="table-scroll">
        <table class="table">
            <thead>
                <tr>
                    <th>Actions</th>
                    <th>Name</th>
                    <th>Amount</th>
                    <th>Order</th>
                    <th>Status</th>
                    <th>Details</th>
                </tr>
            </thead>
            <tbody>
                    @foreach (var package in Model.Packages)
                    {
                        <tr>
                            <td>
                                <div style="display:flex;gap:0.35rem;flex-wrap:wrap;">
                                    <a class="btn btn-secondary btn-xs" href="~/AssociationAdmin/EditSponsorship/@package.Id" title="Edit this package">Edit</a>
                                    <form method="post" asp-page-handler="ToggleActive" asp-route-id="@package.Id" class="inline-form">
                                        <button type="submit" class="btn btn-secondary btn-xs" title="@(package.IsActive ? "Deactivate this package" : "Activate this package")">@(package.IsActive ? "Deactivate" : "Activate")</button>
                                    </form>
                                    <form method="post" asp-page-handler="Delete" asp-route-id="@package.Id" class="inline-form" onsubmit="return confirm('Delete this sponsorship package?');">
                                        <button type="submit" class="btn btn-danger btn-xs" title="Permanently delete this package">Delete</button>
                                    </form>
                                </div>
                            </td>
                            <td><strong>@package.Name</strong></td>
                            <td>@package.Amount.ToString("C")</td>
                            <td>@package.DisplayOrder</td>
                            <td>
                                @if (package.IsActive)
                                {
                                    <span class="status-badge status-badge--green">Active</span>
                                }
                                else
                                {
                                    <span class="status-badge status-badge--gray">Inactive</span>
                                }
                            </td>
                            <td>@(string.IsNullOrWhiteSpace(package.Description) ? "-" : package.Description)</td>
                        </tr>
                    }
            </tbody>
        </table>
        </div>
    }

    <div class="admin-back-nav">
        <a class="btn btn-secondary" href="~/AssociationAdmin">&#8592; Back to Dashboard</a>
    </div>
</section>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
