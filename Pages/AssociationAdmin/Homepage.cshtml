@page
@model GolfAssociationCommunity.Pages.AssociationAdmin.HomepageModel
@{
    ViewData["Title"] = "Homepage Content";
}

<section class="section-panel">
    <h1>Homepage Content</h1>
    <p>Manage what visitors see on your association's public homepage — hero, gallery, sponsors, and charity.</p>
    @if (TempData["SuccessMessage"] is string success)
    {
        <div class="message success">@success</div>
    }
    @if (TempData["ErrorMessage"] is string error)
    {
        <div class="message error">@error</div>
    }
</section>

@* Hero & Branding *@
<section class="section-panel">
    <h2>Hero &amp; Branding</h2>
    <p>Set the banner photo, tagline, motto, and founding year shown at the top of your homepage.</p>

    <form method="post" asp-page-handler="SaveHero" enctype="multipart/form-data" class="form-shell">
        <div asp-validation-summary="ModelOnly" class="message error"></div>
        <input type="hidden" asp-for="HeroSettings.ExistingHeroImageUrl" />

        <div class="form-section">
            <div class="form-grid">
                <div class="full">
                    <label asp-for="HeroSettings.Description">Description / About (shown in hero)</label>
                    <textarea asp-for="HeroSettings.Description" class="form-control" rows="3"></textarea>
                </div>
                <div class="full">
                    <label asp-for="HeroSettings.Tagline">Tagline <span class="field-note">Short bold phrase, e.g. "Where Golf Lives"</span></label>
                    <input asp-for="HeroSettings.Tagline" class="form-control" placeholder="Where Golf Lives" />
                </div>
                <div class="full">
                    <label asp-for="HeroSettings.Motto">Motto / Quote <span class="field-note">Appears as a quote in the hero</span></label>
                    <input asp-for="HeroSettings.Motto" class="form-control" placeholder="Good shot or bad — we're still brothers." />
                </div>
                <div>
                    <label asp-for="HeroSettings.EstYear">Year Established</label>
                    <input asp-for="HeroSettings.EstYear" type="number" class="form-control" placeholder="2008" />
                </div>
                <div>
                    <label asp-for="HeroImage">Hero Banner Photo <span class="field-note">JPG/PNG/WebP, max 5 MB</span></label>
                    @if (!string.IsNullOrWhiteSpace(Model.HeroSettings.ExistingHeroImageUrl))
                    {
                        <div class="form-meta">
                            <img src="@Model.HeroSettings.ExistingHeroImageUrl" alt="Current hero" style="height:72px;border-radius:4px;" />
                            <span class="field-note">Upload to replace.</span>
                        </div>
                    }
                    <input asp-for="HeroImage" type="file" accept="image/*" class="form-control" />
                </div>
                <div>
                    <label asp-for="HeroVideo">Hero Background Video <span class="field-note">MP4/WebM/MOV, max 100 MB (optional — plays behind hero text)</span></label>
                    @if (!string.IsNullOrWhiteSpace(Model.HeroSettings.ExistingHeroVideoUrl))
                    {
                        <div class="form-meta" style="align-items:center;gap:1rem;">
                            <video src="@Model.HeroSettings.ExistingHeroVideoUrl" style="height:72px;border-radius:4px;" muted></video>
                            <label class="checkbox-label" style="margin:0;">
                                <input asp-for="ClearHeroVideo" type="checkbox" />
                                Remove current video
                            </label>
                        </div>
                    }
                    <input asp-for="HeroVideo" type="file" accept="video/mp4,video/webm,video/quicktime" class="form-control" />
                    <span asp-validation-for="HeroVideo" class="text-danger"></span>
                </div>
            </div>
        </div>
        <div class="form-actions">
            <button type="submit" class="btn">Save Hero &amp; Branding</button>
        </div>
    </form>
</section>

@* Gallery *@
<section class="section-panel">
    <h2>Gallery</h2>

    <div class="form-grid" style="margin-bottom:1.5rem;">
@* Photo upload *@
        <div class="form-section">
            <h3 class="form-section-title">Add Photo</h3>
            <form method="post" asp-page-handler="AddPhoto" enctype="multipart/form-data" class="form-shell">
                <div>
                    <label asp-for="PhotoCaption">Caption (optional)</label>
                    <input asp-for="PhotoCaption" class="form-control" placeholder="Season opener 2025" />
                </div>
                <div>
                    <label asp-for="PhotoUpload">Photo <span class="field-note">JPG/PNG/WebP, max 5 MB</span></label>
                    <input asp-for="PhotoUpload" type="file" accept="image/*" class="form-control" required />
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn">Upload Photo</button>
                </div>
            </form>
        </div>

@* Video embed *@
        <div class="form-section">
            <h3 class="form-section-title">Add Video</h3>
            <form method="post" asp-page-handler="AddVideo" class="form-shell">
                <div>
                    <label asp-for="VideoCaption">Caption (optional)</label>
                    <input asp-for="VideoCaption" class="form-control" placeholder="Highlights from the championship" />
                </div>
                <div>
                    <label asp-for="VideoUrl">YouTube or Vimeo URL</label>
                    <input asp-for="VideoUrl" type="url" class="form-control" placeholder="https://www.youtube.com/watch?v=..." />
                </div>
                <div>
                    <label class="checkbox-label">
                        <input asp-for="VideoAutoPlay" type="checkbox" />
                        Auto-play when page loads
                    </label>
                    <p class="field-note">Note: browsers may block autoplay unless the video is muted.</p>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn">Add Video</button>
                </div>
            </form>
        </div>
    </div>

    @{
        var adminPhotos = Model.MediaItems.Where(m => m.MediaType == GolfAssociationCommunity.Models.MediaType.Photo).ToList();
        var adminVideos = Model.MediaItems.Where(m => m.MediaType == GolfAssociationCommunity.Models.MediaType.Video).ToList();
    }

    @if (adminPhotos.Count == 0 && adminVideos.Count == 0)
    {
        <p>No gallery items yet. Upload photos or add video links above.</p>
    }

    @if (adminPhotos.Count > 0)
    {
        <h3 style="margin-top:1.5rem;">Photos</h3>
        <div class="gallery-admin-grid">
            @foreach (var item in adminPhotos)
            {
                <div class="gallery-admin-item">
                    <img src="@item.Url" alt="@item.Caption" class="gallery-admin-thumb" />
                    @if (!string.IsNullOrWhiteSpace(item.Caption))
                    {
                        <p class="field-note" style="margin:0.25rem 0;">@item.Caption</p>
                    }
                    <form method="post" asp-page-handler="DeleteMedia" asp-route-id="@item.Id"
                          onsubmit="return confirm('Remove this photo?')">
                        <button type="submit" class="btn btn-secondary" style="margin-top:0.5rem;padding:0.35rem 0.75rem;font-size:0.85rem;">Remove</button>
                    </form>
                </div>
            }
        </div>
    }

    @if (adminVideos.Count > 0)
    {
        <h3 style="margin-top:1.5rem;">Videos</h3>
        <div class="gallery-admin-grid">
            @foreach (var item in adminVideos)
            {
                <div class="gallery-admin-item">
                    <div class="gallery-admin-video-icon">&#9654; Video</div>
                    @if (!string.IsNullOrWhiteSpace(item.Caption))
                    {
                        <p class="field-note" style="margin:0.25rem 0;">@item.Caption</p>
                    }
                    <p class="field-note" style="margin:0;">Auto-play: @(item.AutoPlay ? "Yes" : "No")</p>
                    <form method="post" asp-page-handler="DeleteMedia" asp-route-id="@item.Id"
                          onsubmit="return confirm('Remove this video?')">
                        <button type="submit" class="btn btn-secondary" style="margin-top:0.5rem;padding:0.35rem 0.75rem;font-size:0.85rem;">Remove</button>
                    </form>
                </div>
            }
        </div>
    }
</section>

@* Sponsors *@
<section id="sponsors" class="section-panel">
    <h2>Sponsors</h2>
    <p>Add sponsor logos to your homepage. These are display-only entries separate from payment packages.</p>

    <form method="post" asp-page-handler="AddSponsor" enctype="multipart/form-data" class="form-shell" style="margin-bottom:1.5rem;">
        <div asp-validation-summary="ModelOnly" class="message error"></div>
        <div class="form-section">
            <div class="form-grid">
                <div>
                    <label asp-for="NewSponsor.Name">Sponsor Name</label>
                    <input asp-for="NewSponsor.Name" class="form-control" placeholder="Acme Golf Co." />
                    <span asp-validation-for="NewSponsor.Name" class="text-danger"></span>
                </div>
                <div>
                    <label asp-for="NewSponsor.Category">Category <span class="field-note">e.g. FOOD, INSURANCE</span></label>
                    <input asp-for="NewSponsor.Category" class="form-control" placeholder="FOOD" />
                </div>
                <div>
                    <label asp-for="NewSponsor.Website">Website (optional)</label>
                    <input asp-for="NewSponsor.Website" type="url" class="form-control" placeholder="https://example.com" />
                </div>
                <div>
                    <label asp-for="NewSponsor.DisplayOrder">Display Order</label>
                    <input asp-for="NewSponsor.DisplayOrder" type="number" class="form-control" />
                </div>
                <div class="full">
                    <label asp-for="SponsorLogo">Logo <span class="field-note">JPG/PNG/WebP, max 5 MB (optional)</span></label>
                    <input asp-for="SponsorLogo" type="file" accept="image/*" class="form-control" />
                    <span asp-validation-for="SponsorLogo" class="text-danger"></span>
                </div>
            </div>
        </div>
        <div class="form-actions">
            <button type="submit" class="btn">Add Sponsor</button>
        </div>
    </form>

    @if (Model.EditSponsorId.HasValue)
    {
        var editTarget = Model.Sponsors.FirstOrDefault(s => s.Id == Model.EditSponsorId.Value);
        @if (editTarget != null)
        {
            <div id="sponsors" class="form-shell" style="margin-bottom:1.5rem;border:2px solid var(--color-primary,#2563eb);border-radius:8px;padding:1.25rem;">
                <h3 style="margin-top:0;">Editing: @editTarget.Name</h3>
                <form method="post" asp-page-handler="EditSponsor" asp-route-id="@Model.EditSponsorId" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="message error"></div>
                    <div class="form-section">
                        <div class="form-grid">
                            <div>
                                <label asp-for="EditSponsor.Name">Sponsor Name</label>
                                <input asp-for="EditSponsor.Name" class="form-control" />
                                <span asp-validation-for="EditSponsor.Name" class="text-danger"></span>
                            </div>
                            <div>
                                <label asp-for="EditSponsor.Category">Category <span class="field-note">e.g. FOOD, INSURANCE</span></label>
                                <input asp-for="EditSponsor.Category" class="form-control" />
                            </div>
                            <div>
                                <label asp-for="EditSponsor.Website">Website (optional)</label>
                                <input asp-for="EditSponsor.Website" type="url" class="form-control" />
                            </div>
                            <div>
                                <label asp-for="EditSponsor.DisplayOrder">Display Order</label>
                                <input asp-for="EditSponsor.DisplayOrder" type="number" class="form-control" />
                            </div>
                            <div class="full">
                                <label asp-for="EditSponsorLogo">Replace Logo <span class="field-note">JPG/PNG/WebP, max 5 MB (optional)</span></label>
                                @if (!string.IsNullOrWhiteSpace(editTarget.LogoUrl))
                                {
                                    <img src="@editTarget.LogoUrl" alt="@editTarget.Name" style="display:block;height:48px;margin-bottom:.5rem;object-fit:contain;" />
                                }
                                <input asp-for="EditSponsorLogo" type="file" accept="image/*" class="form-control" />
                                <span asp-validation-for="EditSponsorLogo" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn">Save Changes</button>
                        <a href="~/AssociationAdmin/Homepage#sponsors" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        }
    }

    @if (Model.Sponsors.Count == 0)
    {
        <p>No sponsors added yet.</p>
    }
    else
    {
        <div class="table-scroll">
            <table class="table">
                <thead>
                    <tr>
                        <th>Logo</th>
                        <th>Name</th>
                        <th>Category</th>
                        <th>Website</th>
                        <th>Order</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var s in Model.Sponsors)
                    {
                        <tr>
                            <td>
                                @if (!string.IsNullOrWhiteSpace(s.LogoUrl))
                                {
                                    <img src="@s.LogoUrl" alt="@s.Name" style="height:40px;width:40px;object-fit:contain;" />
                                }
                                else
                                {
                                    <span class="sponsor-initials-sm">@(s.Name.Length >= 2 ? s.Name[..2].ToUpperInvariant() : s.Name.ToUpperInvariant())</span>
                                }
                            </td>
                            <td>@s.Name</td>
                            <td>@(s.Category ?? "—")</td>
                            <td>
                                @if (!string.IsNullOrWhiteSpace(s.Website))
                                {
                                    <a href="@s.Website" target="_blank" rel="noopener noreferrer">@s.Website</a>
                                }
                                else { <span>—</span> }
                            </td>
                            <td>@s.DisplayOrder</td>
                            <td>
                                <div style="display:flex;gap:.5rem;flex-wrap:wrap;">
                                    <a href="?editId=@s.Id#sponsors" class="btn btn-secondary">Edit</a>
                                    <form method="post" asp-page-handler="DeleteSponsor" asp-route-id="@s.Id"
                                          onsubmit="return confirm('Remove @s.Name?')">
                                        <button type="submit" class="btn btn-secondary">Remove</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</section>

@* Charities *@
<section id="charities" class="section-panel">
    <h2>Charities</h2>
    <p>Add charities your association supports. Each entry appears on the public homepage.</p>

    <form method="post" asp-page-handler="AddCharity" enctype="multipart/form-data" class="form-shell" style="margin-bottom:1.5rem;">
        <div asp-validation-summary="ModelOnly" class="message error"></div>
        <div class="form-section">
            <div class="form-grid">
                <div>
                    <label asp-for="NewCharity.Name">Charity Name</label>
                    <input asp-for="NewCharity.Name" class="form-control" placeholder="Local Youth Golf Foundation" />
                    <span asp-validation-for="NewCharity.Name" class="text-danger"></span>
                </div>
                <div>
                    <label asp-for="NewCharity.Url">Website (optional)</label>
                    <input asp-for="NewCharity.Url" type="url" class="form-control" placeholder="https://charity.example.com" />
                </div>
                <div>
                    <label asp-for="NewCharity.DisplayOrder">Display Order</label>
                    <input asp-for="NewCharity.DisplayOrder" type="number" class="form-control" />
                </div>
                <div class="full">
                    <label asp-for="NewCharity.Description">Description</label>
                    <textarea asp-for="NewCharity.Description" class="form-control" rows="3" placeholder="We proudly support..."></textarea>
                </div>
                <div class="full">
                    <label asp-for="CharityImage">Photo <span class="field-note">JPG/PNG/WebP, max 5 MB (optional)</span></label>
                    <input asp-for="CharityImage" type="file" accept="image/*" class="form-control" />
                    <span asp-validation-for="CharityImage" class="text-danger"></span>
                </div>
            </div>
        </div>
        <div class="form-actions">
            <button type="submit" class="btn">Add Charity</button>
        </div>
    </form>

    @if (Model.EditCharityId.HasValue)
    {
        var editTarget = Model.Charities.FirstOrDefault(c => c.Id == Model.EditCharityId.Value);
        @if (editTarget != null)
        {
            <div class="form-shell" style="margin-bottom:1.5rem;border:2px solid var(--color-primary,#2563eb);border-radius:8px;padding:1.25rem;">
                <h3 style="margin-top:0;">Editing: @editTarget.Name</h3>
                <form method="post" asp-page-handler="EditCharity" asp-route-id="@Model.EditCharityId" enctype="multipart/form-data">
                    <div asp-validation-summary="ModelOnly" class="message error"></div>
                    <div class="form-section">
                        <div class="form-grid">
                            <div>
                                <label asp-for="EditCharity.Name">Charity Name</label>
                                <input asp-for="EditCharity.Name" class="form-control" />
                                <span asp-validation-for="EditCharity.Name" class="text-danger"></span>
                            </div>
                            <div>
                                <label asp-for="EditCharity.Url">Website (optional)</label>
                                <input asp-for="EditCharity.Url" type="url" class="form-control" />
                            </div>
                            <div>
                                <label asp-for="EditCharity.DisplayOrder">Display Order</label>
                                <input asp-for="EditCharity.DisplayOrder" type="number" class="form-control" />
                            </div>
                            <div class="full">
                                <label asp-for="EditCharity.Description">Description</label>
                                <textarea asp-for="EditCharity.Description" class="form-control" rows="3"></textarea>
                            </div>
                            <div class="full">
                                <label asp-for="EditCharityImage">Replace Photo <span class="field-note">JPG/PNG/WebP, max 5 MB (optional)</span></label>
                                @if (!string.IsNullOrWhiteSpace(editTarget.ImageUrl))
                                {
                                    <img src="@editTarget.ImageUrl" alt="@editTarget.Name" style="display:block;height:64px;margin-bottom:.5rem;object-fit:contain;" />
                                }
                                <input asp-for="EditCharityImage" type="file" accept="image/*" class="form-control" />
                                <span asp-validation-for="EditCharityImage" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn">Save Changes</button>
                        <a href="~/AssociationAdmin/Homepage#charities" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        }
    }

    @if (Model.Charities.Count == 0)
    {
        <p>No charities added yet.</p>
    }
    else
    {
        <div class="table-scroll">
            <table class="table">
                <thead>
                    <tr>
                        <th>Photo</th>
                        <th>Name</th>
                        <th>Website</th>
                        <th>Order</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var c in Model.Charities)
                    {
                        <tr>
                            <td>
                                @if (!string.IsNullOrWhiteSpace(c.ImageUrl))
                                {
                                    <img src="@c.ImageUrl" alt="@c.Name" style="height:40px;width:40px;object-fit:contain;" />
                                }
                                else
                                {
                                    <span class="sponsor-initials-sm">@(c.Name.Length >= 2 ? c.Name[..2].ToUpperInvariant() : c.Name.ToUpperInvariant())</span>
                                }
                            </td>
                            <td>@c.Name</td>
                            <td>
                                @if (!string.IsNullOrWhiteSpace(c.Url))
                                {
                                    <a href="@c.Url" target="_blank" rel="noopener noreferrer">@c.Url</a>
                                }
                                else { <span>—</span> }
                            </td>
                            <td>@c.DisplayOrder</td>
                            <td>
                                <div style="display:flex;gap:.5rem;flex-wrap:wrap;">
                                    <a href="?editCharityId=@c.Id#charities" class="btn btn-secondary">Edit</a>
                                    <form method="post" asp-page-handler="DeleteCharity" asp-route-id="@c.Id"
                                          onsubmit="return confirm('Remove @c.Name?')">
                                        <button type="submit" class="btn btn-secondary">Remove</button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</section>
