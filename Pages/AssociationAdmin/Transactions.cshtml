@page
@model GolfAssociationCommunity.Pages.AssociationAdmin.TransactionsModel
@{
    ViewData["Title"] = "Authorize.Net Transactions";
}

<section class="section-panel">
    <div class="admin-page-header">
        <div>
            <h1>Transactions</h1>
            <p>Review registrations and sponsorship payments. Open a transaction to reconcile or refund it.</p>
        </div>
    </div>

    @if (TempData["SuccessMessage"] is string successMessage)
    {
        <div class="message success">@successMessage</div>
    }

    @if (TempData["ErrorMessage"] is string errorMessage)
    {
        <div class="message error">@errorMessage</div>
    }

    <form method="get">
        <div class="admin-filter-bar">
            <div>
                <label for="search">Search</label>
                <input id="search" name="Search" value="@Model.Search" class="form-control" placeholder="Transaction, customer, source" />
            </div>
            <div class="admin-filter-bar__actions">
                <button type="submit" class="btn">Apply</button>
                <a class="btn btn-secondary" href="~/AssociationAdmin/Transactions">Reset</a>
            </div>
        </div>
    </form>

    <div class="card-grid">
        <article class="card">
            <h3>@Model.Transactions.Count</h3>
            <p>Transactions</p>
        </article>
        <article class="card">
            <h3>@Model.TotalAmount.ToString("C")</h3>
            <p>Total captured</p>
        </article>
        <article class="card">
            <h3>@Model.RefundedAmount.ToString("C")</h3>
            <p>Refunded</p>
        </article>
        <article class="card">
            <h3>@Model.RefundedCount</h3>
            <p>Refunded records</p>
        </article>
    </div>

    @if (Model.Transactions.Count == 0)
    {
        <p>No transactions found.</p>
    }
    else
    {
        <div class="table-scroll">
        <table class="table">
            <thead>
                <tr>
                    <th>Actions</th>
                    <th>Type</th>
                    <th>Customer</th>
                    <th>Source</th>
                    <th>Paid</th>
                    <th>Status</th>
                    <th>Amount</th>
                    <th>Card</th>
                    <th>Refund</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var transaction in Model.Transactions)
            {
                <tr>
                    <td>
                        <a class="btn btn-secondary btn-xs" asp-page="/AssociationAdmin/Transactions/Details" asp-route-sourceType="@transaction.SourceType" asp-route-id="@transaction.SourceId" title="View full transaction details">View Details</a>
                    </td>
                    <td>@transaction.SourceType</td>
                    <td>
                        <strong>@transaction.CustomerName</strong><br />
                        <span>@transaction.CustomerEmail</span>
                    </td>
                    <td>@transaction.SourceLabel</td>
                    <td>@(transaction.PaidAtUtc.HasValue ? transaction.PaidAtUtc.Value.ToString("MMM d, yyyy") : "-")</td>
                    <td>@transaction.StatusLabel</td>
                    <td>@transaction.Amount.ToString("C")</td>
                    <td>@(string.IsNullOrWhiteSpace(transaction.CardLast4) ? "-" : $"**** {transaction.CardLast4}")</td>
                    <td>
                        @if (transaction.IsRefunded)
                        {
                            <span>@(transaction.RefundAmount.HasValue ? transaction.RefundAmount.Value.ToString("C") : "Refunded")</span><br />
                            @if (transaction.RefundedAtUtc.HasValue)
                            {
                                <span>@transaction.RefundedAtUtc.Value.ToString("MMM d, yyyy")</span>
                            }
                        }
                        else
                        {
                            <span>-</span>
                        }
                    </td>
                </tr>
            }
            </tbody>
        </table>
        </div>
    }

    <div class="admin-back-nav">
        <a class="btn btn-secondary" href="~/AssociationAdmin">&#8592; Back to Dashboard</a>
    </div>
</section>
